# Flutter App – Main Dart File

This repository contains the `main.dart` file from a Flutter project.  
It serves as the entry point of the application and is responsible for initializing the app, setting up the theme, and managing navigation and state.

---

## 🧩 About the Project

This Flutter project is designed to demonstrate the structure and functionality of a typical Flutter app’s `main.dart` file.  
It includes configuration for widgets, routes, and theme management.  
You can clone or use this as a starting point for your own Flutter applications.

---

## ⚙️ Setup Instructions

Follow these steps to run the app locally:

1. **Install Flutter SDK**  
   - Make sure you have Flutter installed on your system.  
   - [Download Flutter](https://flutter.dev/docs/get-started/install)

2. **Clone the Repository**
   ```bash
   git clone https://github.com/azlanabdulla/aster-lend.git
   cd aster-lend
   ```
3. **Get Flutter Dependencies**
   ```bash
   flutter pub get
   ```
4. **Run the Application**
   ```bash
   flutter run
   ```

## 🏗️ Project Structure

lib/
├── main.dart        # Entry point of the application

## 🧑‍💻 Author
```bash
azlanabdulla
